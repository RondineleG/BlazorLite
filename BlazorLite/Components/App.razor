
@using BlazorLite.Data
@using BlazorLite.Models

@inject BlazorLite.Data.AppDataContext _appDataContext



<div class="row mb-3">
    <div class="col-6">
        <h3>Novo Desenvolvedor</h3>
        <div>

            <label>Nome:</label><br />
            <input type="text" id="name" @bind="@pessoa.Name" class="form-control" />
            <br />
            <label>Especialidade:</label><br />
            <textarea @bind="@pessoa.Description" class="form-control"></textarea>
            <br />

            @if (ShowConfirmEdit)
            {
                <button class="btn btn-primary btn-lg" @onclick="ConfirmEditDev">Editar</button>
            }
            else
            {
                <button type="submit" class="btn btn-primary btn-lg" @onclick="AddDev">Cadastrar</button>
            }


        </div>
    </div>
</div>


<table class="table table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Especialidade</th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Pessoas.Count; i++)
        {
            var pessoa = Pessoas[i];
            <tr>

                <td>@(i + 1)</td>
                <td>@pessoa.Id</td>
                <td>@pessoa.Name</td>
                <td>@pessoa.Description</td>

                <td>
                    <a class="btn btn-light">Details</a>
                    <a class="btn btn-danger"   @onclick="()=>RemoveDev(pessoa)">Delete</a>
                    <a class="btn btn-primary"  @onclick="()=>EditDev(pessoa)">Edit</a>
                </td>
            </tr>
        }

    </tbody>
</table>

@code{
    private Pessoa pessoa = new Pessoa();
    bool ShowConfirmEdit;
    public int counter;

    public IList<Pessoa> Pessoas { get; set; }

    protected override void OnInitialized()
    {
        counter = 1;
        ShowConfirmEdit = false;
        Pessoas = _db.Pessoas.ToList();
    }

    private void AddDev()
    {
        if (ShowConfirmEdit)
        {
            return;
        }
        _db.Pessoas.Add(pessoa);
        _db.SaveChanges();

        Pessoas.Add(pessoa);
        pessoa = new Pessoa();
    }

    private void RemoveDev(Pessoa bug)
    {
        _db.Pessoas.Remove(bug);
        _db.SaveChanges();

        Pessoas.Remove(bug);
    }

    private void EditDev(Pessoa bug)
    {
        ShowConfirmEdit = true;
        pessoa = bug;
    }

    private async Task ConfirmEditDev()
    {
        _db.Pessoas.Update(pessoa);
        await _db.SaveChangesAsync();
        pessoa = new Pessoa();
    }


}